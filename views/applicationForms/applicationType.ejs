<% pageTitle = "Choose a service - Get a document legalised – GOV.UK" %>

<div class="inner_header <%if (!user_data.loggedIn){%>no-user-signed-in<%}%>" >

    <%if(back_link){%> <a href="<%= back_link%>" class="back-to-previous govuk-link">Back</a><%}%>
    <% if (user_data.loggedIn) { %>
        <%- partial ('../partials/inner-header.ejs')%>
    <% }else{%>
        <div class="inner-header-content">
            <span class="email-logout-text">
                 <a  href="<%= userServiceURL%>/sign-in?next=serviceSelector&from=start" class="govuk-link" id="sign-in-link" >Sign in</a>
            </span>

        </div>
    <%} %>
</div>

<h1 class="heading-xlarge">Choose a service</h1>

<div class="column-two-thirds">
    <%if(user_data.user && user_data.user.dropOffEnabled){%>
        <div class="form-group">
            <h2 class="heading-medium">Business drop-off service</h2>
            <ul>
                <li>£30 per document</li>
                <li>Milton Keynes drop-off and collection only</li>
                <li>overnight service</li>
                <li>the counter opens on working days from 1pm to 2pm</li>

            </ul>
            <a class="govuk-button govuk-button--secondary" id="dropoff-service" href="/new-application?app_type_group=3">Start a business drop-off application</a>
        </div>
    <%}%>

    <div class="form-group">
        <h2 class="heading-medium nomargintop">Standard service</h2>
            <ul>
                <li>Due to the impact of coronavirus (COVID-19) turnaround times may be longer than usual</li>
                <li>£30 per document</li>
                <li>Documents sent and returned by post</li>
            </ul>

       <%if(!user_data.loggedIn){%>
        <p>If you're a regular customer you can <a href="<%= userServiceURL%>/register?next=serviceSelector&from=start" class="govuk-link">create an account</a> to speed up your applications (optional).</p>
       <%}%>

        <a class="govuk-button govuk-button--secondary" id="standard-service" href="/new-application?app_type_group=1">Start a standard application</a>
    </div>


        <div class="form-group  <%if(user_data.user && !user_data.user.premiumEnabled){%>hide<%}%>">
            <h2 class="heading-medium nomargintop">Premium service</h2>
            <ul>
                <li>For businesses only</li>
                <li>£75 per document</li>
                <li>London drop-off and collection only</li>
                <li>The counter opens on working days, 9am to 1pm for drop-off and 2pm to 4:45pm for collection</li>
                <li>Same-day service for applications received before 1pm</li>
            </ul>
            <%if(!user_data.loggedIn && !user_data.user.premiumEnabled){%>
                <p>To use the premium service you need to <a href="<%= userServiceURL%>/register?next=serviceSelector&from=start" class="create-account-link govuk-link">create an account</a>. You'll be asked to provide company details.</p>
            <%}%>
            <a class="govuk-button govuk-button--secondary" id="premium-service" href="/new-application?app_type_group=2">Start a premium application</a>
        </div>

        <div class="contact">
        <h2 class="heading-medium nomargintop">Contact us</h2>
        <p><a class="govuk-link" href="mailto:legalisationenquiries@fcdo.gov.uk">legalisationenquiries@fcdo.gov.uk</a> <br>
            Telephone: 03700 00 22 44 <br>
            Monday to Friday, 10am to 4pm<br>
            <a class="govuk-link" href="https://www.gov.uk/call-charges" rel="external"  target="_blank">Find out about call charges <span class="visually-hidden"> opens in new window</span></a>
        </p>
        </div>

</div>
