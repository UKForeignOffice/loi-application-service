const FileDownloadController = {
    downloadFileHandler(req, res) {
        // make api call to casebookR
        // .../downløadApostille?timestampe=[time]&apostilleReferenece=[value<get from query params>] GET
    }
}